# Direct runtime dependencies are pinned so local, CI, and production installs
# resolve the same versions. Dependabot keeps these pins current.
Django==6.0.7
django-otp==1.7.0
gunicorn==26.0.0
whitenoise==6.12.0
psycopg2-binary==2.9.12
dj-database-url==3.1.2
python-dotenv==1.2.2
Pillow==12.3.0
qrcode==8.2
cryptography==49.0.0

# Plaid is used for the sandbox bank-link demo. Credentials stay in environment
# secrets and are never sent to the browser.
plaid-python==42.0.0

# SMS reminders are optional and remain inactive without Twilio credentials.
twilio==9.10.9
