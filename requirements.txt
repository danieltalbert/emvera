# Core Django
Django>=6.0

# Django REST Framework
djangorestframework

# OTP / TOTP two-factor authentication
django-otp
pyotp

# Production WSGI server
gunicorn

# PostgreSQL adapter
psycopg2-binary

# Environment variable loading
python-dotenv

# Image handling
Pillow

# QR code generation (for 2FA TOTP setup)
qrcode

# Fernet encryption for Plaid access tokens at rest
cryptography

# Plaid bank integration (optional; only needed if PLAID_CLIENT_ID is set)
# plaid-python
